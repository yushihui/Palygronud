import {ParserExceptionPipePipe} from './parser-exception-pipe.pipe';

describe('ParserExceptionPipePipe', () => {
  it('create an instance', () => {
    const pipe = new ParserExceptionPipePipe();
    expect(pipe).toBeTruthy();
  });
});
